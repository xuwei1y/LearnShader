using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Testjet : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        int i = 0;
        //随便加个注释 测试git
        Debug.LogError(i);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
